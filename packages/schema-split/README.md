# @openbindings/schema-split

`ob-schema-split`: an operation's declared input and output schemas as a
full-width split of static highlighted code blocks — the reference tier of
machine text, presented in the same column geometry as an invocation
cockpit so contract and value align vertically.
